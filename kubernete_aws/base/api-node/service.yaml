apiVersion: v1
kind: Service
metadata:
  name: api-node
  namespace: microservices-demo
  labels:
    app: api-node
spec:
  selector:
    app: api-node
  ports:
  - port: 3001
    targetPort: 3001
    name: http
