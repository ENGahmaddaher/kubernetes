apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: microservices-demo
  labels:
    app: postgres
spec:
  clusterIP: None          # Headless Service - مهم لـ StatefulSet!
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
