apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-react
  namespace: microservices-demo
  labels:
    app: client-react
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client-react
  template:
    metadata:
      labels:
        app: client-react
    spec:
      containers:
      - name: client-react
        image: entransistor/client-react:436c63e8f484be9cfc3808e7a20c96e8f8c85cfb
        ports:
        - containerPort: 8080
          name: http
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
