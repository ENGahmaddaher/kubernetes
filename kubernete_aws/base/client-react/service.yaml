apiVersion: v1
kind: Service
metadata:
  name: client-react
  namespace: microservices-demo
  labels:
    app: client-react
spec:
  type: ClusterIP
  selector:
    app: client-react
  ports:
  - port: 8080
    targetPort: 8080
    name: http
